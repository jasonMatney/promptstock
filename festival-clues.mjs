export const FESTIVAL_CLUES=[
 {id:'kick',title:'Give the world a little nudge',clue:'A globe is waiting at someone’s feet. Three rings, one good kick at a time.',place:'Look on the lawn just inside the festival entrance.'},
 {id:'aid',title:'A friend could use a hand',clue:'Someone at the Reboot Clinic has lost their spark. Match the missing pieces and help them wake up.',place:'Find the red-striped Reboot Clinic tent on the right.'},
 {id:'maker',title:'Make something useful',clue:'A thirsty guest. A missing bag. A little light. The right ingredients make someone’s day.',place:'Try the jars at the large cream tent in the middle.'}
];
export function clueCard(completed){return `<p>The headliner is waiting for three little acts of kindness. Explore the festival and follow these clues in any order.</p><div class="festival-clues">${FESTIVAL_CLUES.map(c=>`<article class="${completed.includes(c.id)?'clue-done':''}"><strong>${completed.includes(c.id)?'✓':'◇'} ${c.title}</strong><p>${completed.includes(c.id)?'You made it happen.':c.clue}</p><small>${c.place}</small></article>`).join('')}</div><p class="callout">${completed.length===3?'All three lights are on. Follow the path behind the tents to the main stage. Your concert is ready.':'Three completed challenges bring everyone together for One Island One View at the main stage.'}</p><p>A different kind of secret waits at Lantern Pier. Someone left a canoe by the dock…</p>`;}
